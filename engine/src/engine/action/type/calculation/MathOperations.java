package engine.action.type.calculation;

import engine.property.type.Type;

public enum MathOperations {
    PLUS, MINUS, TIMES, DIVIDE;

    Object calculate(Object arg1, Object arg2, Type type){
        switch (this){
            case PLUS:
                if(Type)

        }
    }
}
